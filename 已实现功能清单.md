# Card Duel（Python 端）已实现功能清单

本文档汇总 **main 分支** 相对浏览器端分支新增/修正的全部功能，供浏览器端
（web 分支）逐项对照移植。每个条目给出：功能说明、本端代码位置、验证测试。

说明：
- 规则/数值类功能必须与本文描述的行为一致（浏览器端照逻辑实现）；
- 流程/协议类按消息流程实现；
- UI/设置类逻辑相同、表现适配浏览器；
- 本端测试 `tests/` 可作为行为契约，每实现一项就按断言验证。

---

## 一、规则与战斗逻辑

| 功能 | 说明 | 代码位置 | 验证 |
| --- | --- | --- | --- |
| 牌堆双堆循环 | 打出的非消耗牌/弃牌/插入物拔出后返回统一进弃牌堆；抽牌堆空时把弃牌堆洗入；联机收到 `pending_draw_returns` 时：见闻→见闻池、生物→可召唤池、其余→弃牌堆 | `network/protocol.py`、`core/rules.py` | `test_protocol.py` |
| 电矛消耗不返还 | 拔出电矛【插入】(50) 只恢复力量，不向原主人返还普通电矛；钢筋(49)拔出仍返还卡1 | `cards/injected.py`、`cards/slugcat/specs.py` | `test_slugcat.py` |
| 生物死亡即消耗 | 死亡生物不回可召唤池、不进牌堆循环 | `cards/slugcat/creatures.py` | `test_slugcat.py` |
| 未死亡离场回池 | 猫跑路清除的手牌生物回到可召唤池（可再被猫闯祸召唤） | `cards/slugcat/effects.py`、`creatures.py` | `test_slugcat.py` |
| 烈焰蜥蜴只免本回合 | 支付1能量只避免本回合10伤，生物留在手牌 | `cards/slugcat/lifecycle.py` | `test_slugcat.py` |
| 生物不占手牌卡槽 | 生物只在生物区（`hand_creatures`），不占手牌上限；管虫(26)是唯一作为实体卡拿在手里的生物 | `cards/slugcat/creatures.py`、`hand.py`、`network/gameplay.py` | `test_slugcat.py` |
| 打出生物=躲避 | 支付费用后生物离场，回合结束不再出伤；可转移生物（小面条/射线虫/钢秃鹫）仍为转移 | `cards/slugcat/effects.py` | `test_slugcat.py` |
| 秃鹫离场 | 我方生物死亡时，手牌中一只秃鹫(19)离场（相当于耗能2打出，自动不扣能量） | `cards/slugcat/creatures.py` | `test_slugcat.py` |
| 烈焰蜈蚣重做 | 只有一只、共享20血池；表现成多张体节：每张新体节提供一次免伤（红边），免伤后甲壳脱落（黑边）；存活时每回合结束在体节较多一方或双方各加一张，首次召唤加入自己手牌；索敌按一只；烟雾果秒杀整只；手牌体节≥3时造成15伤 | `cards/slugcat/creatures.py`、`lifecycle.py`、`core/models.py` | `test_slugcat.py`（共享池/甲壳/增殖/秒杀/单目标） |
| 免伤双方各自算 | 自己一侧手牌/威胁区有几张红边体节就免伤几次（每张一次），双方独立 | `cards/slugcat/lifecycle.py` | `test_slugcat.py` |
| 回合结束新增生物不出伤 | 回合结束结算先快照在场生物：当回合新增的面条蝇/秃鹫等下个回合结束才出伤 | `cards/slugcat/lifecycle.py` | `test_slugcat.py` |
| 闪光果 | 可选"玩家"致盲（本回合前两张不能是攻击）或转移生物进对方手牌生物区；保留生物持有物品 | `cards/slugcat/effects.py` | `test_slugcat.py` |
| 拾荒者物品 | 生成时随机确定并存储（概率 3/5 钢筋、1/10 炸药、1/5 炸矛、1/10 电矛），加入时播报；攻击/死亡掉落/白珍珠均用该物品 | `cards/slugcat/creatures.py`、`effects.py`、`lifecycle.py` | `test_slugcat.py` |
| 拾荒者好感度 | 给珍珠（白珍珠换物/有色珍珠雇佣）+1，击杀拾荒者-1；好感度>0 时任何拾荒者不攻击你；给珍珠提示"好感度提升"，攻击时播报高/低好感度 | `cards/slugcat/effects.py`、`creatures.py`、`lifecycle.py`、`state.py` | `test_slugcat.py` |
| 攻击管线 | 玩家攻击与生物攻击统一经 `apply_damage`：免疫→防御值→角色修正（甲壳免伤等）→敏捷/防伤→扣血 | `application/combat.py` | `test_slugcat.py` |
| 攻击播报明细 | 攻击行内联"（总伤害X，扣敏捷Y，实际扣血Z）"，生物攻击同格式 | `application/combat.py`、`cards/slugcat/creatures.py`、`lifecycle.py` | `test_slugcat.py` |
| 炸药可选目标 | 10伤走 `resolve_attack`（可选玩家或手牌生物），随后自身5伤 | `cards/slugcat/effects.py` | `test_slugcat.py` |
| 先手首回合不扣血 | 规则开启时，先手方第一回合攻击照常结算（插入/弃牌等效果保留），结算后把后手方血量恢复为攻击前——后手方回合开始保持满血 | `cards/slugcat/creatures.py` | `test_match_flow.py` |

## 生物交互强调要点（设计原则，移植时必须遵守）

- **生物不占手牌卡槽**：生物永远只存在于生物区（`hand_creatures`），不进手牌卡槽、不占手牌上限；**管虫（26）是唯一作为实体卡拿在手里的生物**。
- **生物没死才能再出现**：死亡即消耗（不回可召唤池、不进牌堆循环）；只有**未死亡**的离场（猫跑路）才回到可召唤池，之后可被猫闯祸再次召唤。
- **打出生物 = 躲避**：支付费用后生物离场，本回合结束不再造成伤害；可转移生物（小面条/射线虫/钢秃鹫）的"打出"仍为转移效果。
- **烈焰蜥蜴**：支付 1 能量**只避免本回合**的 10 点伤害，生物留在手牌继续存在——不是移除、也不是返还召唤池。
- **当回合新增的生物不在当次回合结束出伤**：小面条死亡当场引来的面条蝇、射线虫引来的秃鹫等，要等**下一个回合结束**才结算伤害（回合结束先快照在场生物）。
- **秃鹫离场**：我方生物死亡时，手牌中**一只**秃鹫离场，相当于耗能 2 打出（自动触发、不扣能量）。
- **烈焰蜈蚣**：
  - 设定上**只有一只**，共享 20 生命，表现成多张体节卡；
  - **每张新体节提供一次免伤**（红边），免伤后甲壳脱落（黑边）；
  - 存活时每回合结束**在数量较多的一方或双方加入一张**，首次召唤加入自己手牌；
  - **索敌按一只**计算；烟雾果可秒杀整只；
  - 手牌体节数 ≥3 时造成 15 点伤害。
- **免伤双方各自算**：自己一侧（手牌/威胁区）里有几张红边体节就免伤几次，双方独立计算。
- **拾荒者好感度**：给拾荒者珍珠（白珍珠换物/有色珍珠雇佣）+1、击杀拾荒者-1；**好感度 > 0 时任何拾荒者不攻击你**；给珍珠时提示"好感度提升"，回合结束攻击时播报"高/低好感度"。
- **生物攻击同样过防御**：生物回合结束伤害与玩家攻击一样，统一经过目标的免疫→防御值→角色修正→敏捷/防伤；播报内联"（总伤害/扣敏捷/实际扣血）"。
- **生物卡面**：卡面上显示生命值；描边颜色有含义（烈焰蜈蚣红=有甲壳可免伤、黑=甲壳已消耗；新加入的体节都是红边）。

## 二、对局流程

| 功能 | 说明 | 代码位置 | 验证 |
| --- | --- | --- | --- |
| 随机种子发牌 | 主机生成/设定种子并经 `match_seed` 消息同步给客户端；双方用同一 `random.Random(seed)` 洗牌建牌堆——相同选角+相同种子→发牌一致 | `network/setup.py`、`core/rules.py`、`core/models.py`（`GameState.random_seed`） | `test_match_flow.py` |
| 主菜单 | 启动先显示"创建房间/加入房间/退出"；`main.py`/`guest.py` 均可进入两种流程 | `ui/main_menu.py`、`network/launcher.py` | `test_match_flow.py` |
| 房间界面 | 创建/加入房间：下拉选角色、房间聊天（实时互通）、主机可设规则（先手：主机/客机/随机；随机或固定种子；先手方第一回合不扣血开关）；`room_start`/`room_ready` 确认后开局 | `ui/room_dialog.py`、`network/setup.py` | `test_match_flow.py` |
| 先手规则 | 支持客机先手（服务器/客户端回合循环按 `first_player_id` 分支） | `network/server.py`、`client.py` | — |
| 胜负与再来一场 | 对局循环检测 `winning_player_id()`（蛞蝓猫业力归零/战士血量归零）；结束播报"玩家X获胜"；弹窗"再来一场"，双方同意则重置并重开（重新进房间界面），任一方退出则回主菜单 | `network/server.py`、`client.py`、`setup.py` | `test_match_flow.py` |
| 房间设置播报 | 对局开始前双方日志播报：主机/客机角色、先手方、随机种子、首回合不扣血开关 | `network/setup.py` | `test_match_flow.py` |

## 三、UI / 交互

| 功能 | 说明 | 代码位置 | 验证 |
| --- | --- | --- | --- |
| 生物手牌展示 | 生物统一排在手牌最后槽位，卡面带血量；卡面直接绘制彩色描边（普通深色/生物金/插入物红/烈焰蜈蚣按甲壳红黑） | `ui/network_view.py`、`ui/network.py`、`core/resources.py` | `test_ui_logic.py`（描边像素） |
| 对方生物查看器 | 信息栏下方"查看对方生物"按钮，打开非阻塞查看窗（手牌+威胁生物，右键预览） | `ui/opponent_viewer.py` | `test_ui_logic.py` |
| 牌堆查看器 | 左端对齐；滚轮绑定画布及子控件防失效；打开/缩放时画布贴合面板；右键预览按全局槽位对齐 | `ui/deck_viewer.py` | `test_ui_logic.py` |
| 调试控制台 `/tool` | 聊天框输入打开：修改本地玩家参数（生命/能量/力量/毒/防御/角色数值），按分类树选择加入卡牌（生物走真实生成路径）；修改播报给双方 | `ui/debug_tool.py` | `test_ui_logic.py` |
| 设置弹窗 | 多面板可切换：日志文本颜色、卡牌类型边框颜色（调色盘取色，实时生效）、音效设置、操作设置；修复面板切换返回 | `ui/settings_window.py` | `test_ui_logic.py` |
| 设置持久化 | 日志颜色/卡牌边框颜色/音效/操作/房间规则保存到根目录 `settings.json`，启动自动读取；逐键合并保存避免双进程互相覆盖 | `ui/app_settings.py` | `test_ui_logic.py` |
| 日志类型规范 | 六类日志（chat/turn/damage/gain/warn/normal）统一标识号+显示名+默认颜色 | `ui/network_log.py` | `test_ui_logic.py` |
| 音效系统 | Windows `winsound` 非阻塞播放合成 WAV；按日志类型触发（伤害/抽牌/警告/聊天/回合/出牌/点击）；总开关+按类型开关并持久化 | `ui/sound.py`、`ui/network_log.py` | `test_ui_logic.py` |
| 单击/双击出牌 | 操作设置：双击（默认）点一下选中、再点确认；单击点一下直接打出（含生物槽位） | `ui/card_interaction.py` | `test_ui_logic.py` |

## 四、数据与文档

| 项目 | 说明 | 位置 |
| --- | --- | --- |
| 工作簿卡面同步 | `docs/工作簿1.xlsx` 全部卡面文本（E列）与数量（C列）与代码一致（48张卡，含烈焰蜈蚣定稿文本） | `docs/工作簿1.xlsx` |
| 变更记录 | 逐项改动记录 | `log/2026-08-21.md`、`CHANGELOG.md` |
| 测试规模 | `tests/` 共 121 个用例，覆盖上述全部行为 | `tests/` |

---

## 移植建议

1. 先移植**一、规则**（纯逻辑，与平台无关），用本端测试断言逐项核对行为；
2. 再移植**二、流程**（消息协议：`match_seed` / `room_chat` / `room_start` / `room_ready` / `room_exit` / `rematch` / `turn_change` / `state` / `card_played`）；
3. 最后适配**三、UI**（浏览器端用自身组件实现同等交互即可）。
