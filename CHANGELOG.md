# Changelog

本文件记录 Card Duel 对战项目的可见行为变更。仅保留近期条目，
更早的历史可在 git 提交记录中追溯。

## [Unreleased] - 2026-08-20

### Added
- 引入杀戮尖塔风格的「抽牌堆 / 弃牌堆」双堆循环：打出的非消耗牌、
  弃牌阶段弃置的牌、生物死亡返还、插入物拔出后返回等，统一进入
  `discard_pile`；当 `draw_pile` 抽空时自动将弃牌堆洗入抽牌堆。
  解决「弃 2 技能 + 1 物品后下一回合立刻又抽回原牌」的非随机循环问题。
- 主界面状态栏新增弃牌堆计数显示；牌堆查看窗口新增「弃牌堆」分组。

### Changed
- `core/rules.draw_cards` 重写为「按需 reshuffle」循环：抽牌堆空且弃牌堆
  非空时自动洗入，保证每轮抽牌顺序不固定。
- 所有「打出的非消耗牌 / 弃牌 / 效果返还」的目标由 `draw_pile` 改为
  `discard_pile`，避免直接进入下一轮的抽牌序列。
- 见闻场景切换（蛞蝓猫 discovery）重置抽牌堆时同步过滤弃牌堆中已离开
  当前场景的非物品牌，保持场景一致性。

## [0.4.0] - 2026-08-19

### Fixed
- `TURN_END` 阶段结算插入钢筋流血、生物伤害、Karma 回血、敏捷扣减后
  立即调用 `refresh_status + send_game_state`，避免「有播报无数值变化」。
  修改位置：`server.py` / `client.py` 的 `play_active_turn` 在
  `signal_turn_change` 之前补齐同步逻辑。
- 弃牌阶段弃 2 技能 + 1 物品后下一回合重复抽到相同 3 张牌：临时将
  「打出的非消耗牌 / 弃牌」从 `draw_pile.append`（堆顶）改为
  `draw_pile.insert(0, card_id)`（堆底）。本版本进一步用双堆循环彻底解决。
- 修复抽牌堆查看弹窗、手牌右键放大弹窗使用阻塞 `window.read()` 与主窗口
  事件泵冲突导致弹窗立即消失并卡住的问题：统一改为 `window.read(timeout=50)`
  轮询，处理 `TIMEOUT_EVENT` 继续等待，异常时退出避免死循环。

## [0.3.x] - 2026-08

### Added
- 蛞蝓猫角色完整卡组（技能 / 物品 / 见闻 / 生物 / 插入物 共 50 张）。
- Karma 回血、敏捷减伤（仅在实战扣血时消耗）、动能资源系统。
- 见闻牌按 ID 独立累计耗能减 1，有效成本低于卡面时显示深绿色。
- 管虫使见闻牌不占手牌位，手牌上限判定 / 弃牌达标 / 提示文本使用
  `effective_hand_size_for_limit`，UI 显示为「有效/总」双格式。

### Changed
- 生物牌（16-26）不再进入蛞蝓猫初始牌堆，仅通过见闻解锁或卡牌效果
  入手；解锁后的生物计数独立追踪并在牌堆查看器中以金色边框展示。
- 插入物（钢筋 49 / 电矛 50）以红色边框 + [Insert] 文本与普通手牌区分。
- 失去生命播报顺序：扣血 → 结算 Karma → 计算实际扣血量 → 仅在
  actual_loss > 0 时播报「失去生命」 → 触发 Karma 回血时追加「消耗业力
  重返雨中」。
