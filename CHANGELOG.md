# Changelog

本文件记录 Card Duel 对战项目的可见行为变更。仅保留近期条目，
更早的历史可在 git 提交记录中追溯。

## [Unreleased] - 2026-08-21

### Added
- 新增 `frontend/` React + Next.js 浏览器客户端，覆盖 WebSocket 连接、房间创建/
  加入、选角与规则、准备/聊天、权威五阶段牌桌、手牌/生物出牌、弃牌、结束
  回合，以及 `integer / option / card_indexes` 三类事务型选择。
- Web 个性化状态新增公开 `card_catalogs` 和与本人手牌位置对齐的动态
  `card_costs`，前端无需复制角色费用规则，也不会获知对方私有牌区。
- 新增 Card Duel 品牌分享图、Open Graph/X 元数据、响应式桌面/移动布局和
  服务端渲染协议契约测试。
- 新增 FastAPI WebSocket 权威房间服务首个纵切：6 位房间号、创建/加入、
  选角、房主规则、准备开局、聊天、断线清理与健康检查。
- Web 开局复用角色目录、`GameState`、`CombatEngine` 和既有洗牌规则，并为
  双方维护独立牌区；状态按接收玩家裁剪，不泄露对方手牌及待处理私有队列。
- 新增 Web 协议版本 1、`card-duel-web` 启动命令和协议说明文档。
- Web 协议升级至版本 2：新增权威五阶段回合、服务端抽牌与能量、出牌、
  弃牌、结束回合、胜负判定，以及 `choice_required / resolve_choice` 多步选择。
- 卡牌选择采用事务快照：选择不足时回滚动作，回答后从干净状态确定性重放，
  避免重复扣能量、伤害或弃牌；公共与私有结算日志分别投递。
- 引入杀戮尖塔风格的「抽牌堆 / 弃牌堆」双堆循环：打出的非消耗牌、
  弃牌阶段弃置的牌、插入物拔出后返回等，统一进入 `discard_pile`；
  当 `draw_pile` 抽空时自动将弃牌堆洗入抽牌堆。
  解决「弃 2 技能 + 1 物品后下一回合立刻又抽回原牌」的非随机循环问题。
- 主界面状态栏新增弃牌堆计数显示；牌堆查看窗口新增「弃牌堆」分组。

### Changed
- `core/rules.draw_cards` 重写为「按需 reshuffle」循环：抽牌堆空且弃牌堆
  非空时自动洗入，保证每轮抽牌顺序不固定。
- 所有「打出的非消耗牌 / 弃牌 / 效果返还」的目标由 `draw_pile` 改为
  `discard_pile`，避免直接进入下一轮的抽牌序列。
- 见闻场景切换（蛞蝓猫 discovery）重置抽牌堆时同步过滤弃牌堆中已离开
  当前场景的非物品牌，保持场景一致性。

### Fixed
- 允许通过 `127.0.0.1` 访问 Next.js 开发服务器，避免开发资源因来源与
  `localhost` 不同而被拦截，导致页面交互或热更新失效。
- 浏览器前端兼容仍在运行的旧协议 2 后端进程：缺少 `card_catalogs` 或
  `card_costs` 时降级为卡牌编号/静态费用并提示重启，不再在进入对局时因读取
  `undefined` 崩溃。
- 修复日志自动滚动 `useEffect` 隐式返回 Promise，导致 React 在进入大厅卸载
  effect 时抛出 `destroy is not a function` 并显示错误覆盖层的问题。
- 移除 Sites、Cloudflare Worker、vinext 与未使用的数据库示例骨架，改为标准
  Next.js 16 App Router 构建；部署平台只需支持 Node.js/Next.js，WebSocket
  后端通过 `NEXT_PUBLIC_CARD_DUEL_WS_URL` 独立配置。
- 修复合并 `03cc41c` 时 `_apply_local_pending_actions` 被旧分支覆盖导致的
  回归：`pending_draw_returns` 中的非见闻卡重新路由到 `discard_pile`，
  生物卡（16–26）返还到 `unlocked_creature_counts`，不再进入抽牌堆循环。
- 电矛【插入】拔出后不再向原主人返还普通电矛（卡 5）：电矛为消耗牌，
  拔出只恢复对应力量；卡面描述同步更新。
- 修正生物生命周期：**死亡即消耗**，死亡生物不再回到可召唤生物池，
  也不进入抽牌堆/弃牌堆循环；只有**未死亡的离场**（猫跑路了）才回到
  `unlocked_creature_counts`，之后可被猫闯祸再次召唤。
- 修正烈焰蜥蜴：支付 1 点能量只**避免本回合**的 10 点伤害，烈焰蜥蜴
  留在手牌继续存在，不再被移除并返还召唤池。
- 生物不再占手牌卡槽：生物只存在于生物区（`hand_creatures`），不加入
  `hand_cards`，不占手牌上限；唯一例外是管虫（26），作为实体卡拿在手里。
  手牌 UI 将生物统一排在手牌最后几个槽位，描边取生物卡图主色（无图时黑色）。
- 打出生物 = 躲避：支付费用后生物离场，回合结束不再造成伤害；可转移
  生物（小面条/射线虫/钢秃鹫）仍为转移效果。
- 秃鹫：**我方**生物死亡时，手牌中**一只**秃鹫离场（相当于耗能 2 打出，
  自动触发不扣能量）。
- 烈焰蜈蚣重做：**只有一只、共享 20 血池**，表现成多张体节卡。每张体节
  有甲壳可免伤一次（红边）：自己一侧手牌/威胁区里有几张红的就免伤几次，
  双方各自算；免伤后甲壳脱落（黑边），新体节都是红边；存活时每回合增殖
  ——加入体节较多一方，等量则双方各加一张，首次召唤加入自己手牌；索敌
  按一只计算；烟雾果可秒杀整只。

### Added
- 日志新增指令系统：聊天框输入 `/tool` 打开不阻塞的调试控制台，可修改本地
  玩家生命/能量/力量/毒/防御及角色专属数值参数，并选择加入卡牌；每次修改
  本地播报并作为 announcement 广播给对方。
- 修复回合结束结算：小面条死亡当场引来的面条蝇、射线虫引来的秃鹫等
  **当回合新增的生物不再立即出伤**，统一从下个回合结束开始结算。
- 闪光果可自由选择目标：即使自己有生物，也可选择"玩家"将其致盲
  （本回合前两张牌不能是攻击）；选择生物时按表格"移入另一玩家手牌"
  转移进对方**手牌生物区**，并保留其持有物品。
- "对方生物"改为信息栏下方的按钮（同"查看抽牌堆"风格）：点击打开非阻塞
  查看窗，列出对方手牌生物与威胁生物（含血量），右键卡片可放大预览；
  转移过去的牌不再看不见。
- 生物牌与插入物改为**卡面直接绘制彩色描边**（不再依赖按钮高亮，保证可见）：
  普通手牌深色细边；生物金边（无卡图时，烈焰蜈蚣按甲壳红/黑）；插入物
  （49/50）红边；对方生物查看器同样带描边。
- 修复牌堆查看器右键预览：`deck_viewer_card_ids` 改为按全局槽位对齐
  （隐藏槽位占位 None），后半部分分类（见闻池/弃牌堆/可召唤生物等）
  不再出现预览错位或失效。
- 牌堆查看器改为**左端对齐**（卡牌网格与卡槽不再居中）。
- 修复牌堆查看器滚轮有时滚不动：直接绑定滚轮到牌堆画布及其所有子控件
  并返回 `break`，避免 FreeSimpleGUI 多个可滚动列共用全局滚轮绑定导致
  滚动到错误画布或失效。
- 牌堆打开/窗口缩放时按面板实际尺寸重设画布并刷新滚动区域，修复小窗口
  或被系统裁剪时滚轮失效（全屏后正常）的问题。
- 新增"设置"按钮（我方状态卡下方空白处），打开非阻塞设置窗；目前提供
  **日志显示类型管理**：按类别（伤害/获得/警告/回合/聊天/普通）勾选
  显示或隐藏，可扩展后续更多显示设置。
- 日志文本类型规范化：六类日志（chat 聊天 / turn 回合 / damage 伤害 /
  gain 获得 / warn 警告 / normal 普通）统一使用**标识号 + 显示名 + 默认颜色**，
  过滤与着色共用同一套类型定义。
- 设置弹窗改为**可切换的配置界面**（同牌堆查看器切面板）：主界面新增
  "日志文本颜色配置"和"卡牌类型边框色彩配置"两个入口，进入对应配置页
  配置；颜色改用**调色盘按钮**（打开系统取色器，带实时色块预览），不再
  手动输入 HEX；"应用"后立即生效。修复配置页"返回"失效问题（两个配置页
  原来共用一个返回键导致冲突，已改为独立键并用 `sg.pin` 稳定切换面板）。
  卡牌边框支持普通手牌/生物/插入物/烈焰蜈蚣有甲壳/无甲壳五种颜色配置。
- 设置持久化：日志颜色、卡牌边框颜色保存到根目录 `settings.json`
  （已加入 .gitignore），对局启动时自动读取并应用。
- 设置更新即时生效：日志颜色变更后**已显示的日志文本立即重绘**；卡牌
  边框颜色变更后已显示的手牌卡面立即刷新。
- 移除"日志显示类型"过滤功能：日志全部显示，不再按类型隐藏。
- 颜色改为**实时生效**：调色盘选完即应用（日志文本立即重绘），不再需要
  点"应用颜色"确认；通过颜色快照区分真实取色与初始默认值。
- 卡牌边框颜色**真正生效**：蛞蝓猫卡（无真实卡图）在边框颜色变化时
  **强制重绘卡面**，把新颜色烘进卡牌图片（像素级验证）；有真实卡图的
  卡（如战士）不重绘图片，改用按钮描边环显示边框颜色。
- 拾荒者好感度系统：给拾荒者珍珠（白珍珠换物 / 有色珍珠雇佣）+1，
  击杀拾荒者 -1；**好感度>0（给>杀）时任何拾荒者不攻击你**。给珍珠时
  提示"好感度提升"，回合结束攻击时播报"高/低好感度"。
- `/tool` 修复：加入**生物**时改为走真实生成路径（进入生物区
  `hand_creatures`、带血量/甲壳/拾荒者物品），不再塞进手牌卡槽。
- `/tool` 卡牌选择改为**按分类的树形界面**（技能/物品/形态/见闻/生物/
  插入物），选中卡牌显示名称/类型/费用/描述预览，数量 + 加入按钮；不再
  按序号平铺。
- 炸药改为可**选择目标**：10 伤走 `resolve_attack`（可选玩家或手牌生物），
  随后自身 5 伤；不再强制打对方玩家。
- 加入**音效系统**：用 Windows 内置 `winsound` 非阻塞播放程序合成的 WAV
  音效（`assets/sounds/`），按日志类型自动触发——伤害/抽牌/警告/聊天/
  回合/普通点击/出牌各一种音色；设置窗新增"启用音效"开关并持久化，
  仅真实对局入口激活，测试环境静音。
- 修复"启用音效"勾选无效：FreeSimpleGUI 勾选框默认不产生事件，导致开关
  （含持久化）从不触发；已加 `enable_events=True` 并补充"值变化兜底检测"，
  勾选后即时生效并保存到 `settings.json`。
- 新增**主菜单**：启动后先出现"创建房间 / 加入房间 / 退出"，`main.py` 与
  `guest.py` 均可进入两种流程；创建房间界面可选角色并设置随机种子
  （留空随机）。
- 开始逻辑：主机生成/设定种子并通过 `match_seed` 消息同步给客户端，
  双方用**同一种子**洗牌建牌堆——相同选角 + 相同种子 → 发牌一致。
- 结束逻辑：对局循环检测 `winning_player_id`，结束后播报"玩家X获胜"，
  弹窗"再来一场"；双方都同意则**重置对局并重选角色**（主机重新出创建
  房间界面），任一方退出则结束。
- 客机改为**与主机一致的下拉选角**，并新增**房间界面**：进入房间后
  可选角色、可**聊天**（房间阶段实时互通），主机可配置**规则**——
  先手（主机/客机/随机）、随机或固定种子、**先手方第一回合无法造成
  血量损失**。
- "先手方第一回合无法造成血量损失"按简单语义实现：先手方第一回合攻击
  照常结算，但结算后**把后手方血量恢复为攻击前**（后手方回合开始时保持
  满血）；插入、弃牌等其它效果全部保留。
- 主流程调整：`main.py` / `guest.py` 启动 → 主菜单 → 创建/加入 → 房间
  界面（选角+聊天+规则）→ 对局；**对局退出后回到主菜单**，**再来一场
  回到房间界面**。
- 设置新增**音效设置**面板：总开关 + 按类型开关（伤害/抽牌/警告/聊天/
  回合/出牌/点击），勾选即生效并持久化。
- 设置新增**操作设置**面板：**单击出牌 / 双击出牌**（默认双击：点一下
  选中、再点确认；单击：点一下直接打出，含生物），选择即生效并持久化。
- 对局开始前在双方日志**播报具体房间设置**：主机/客机角色、先手方
  （主机或客机）、随机种子、第一回合先手方不扣血是否开启。
- 锁定攻击管线：玩家攻击与生物攻击统一经 `apply_damage`，都经过
  免疫→防御值→角色修正（烈焰蜈蚣甲壳免伤等）→敏捷/防伤逻辑。
- 修复生物攻击播报：回合结束生物伤害的日志不再显示减免前的数值，
  改为按实际结算值播报（如"拾荒者携带钢筋发动攻击" + 实际造成伤害；
  被敏捷完全抵挡时不再报"造成伤害"），避免日志误导。
- 攻击播报统一内联标注结算明细：蛞蝓猫相关攻击（玩家攻击 + 生物攻击）的
  攻击行直接带"（总伤害X，扣敏捷Y，实际扣血Z）"，出伤逻辑不变。
- 拾荒者物品改为**生成时随机确定并存储**：加入时播报携带物品，回合结束
  攻击、死亡掉落、白珍珠获取均使用该物品（概率 3/5 钢筋、1/10 炸药、
  1/5 炸矛、1/10 电矛）。

## [0.4.0] - 2026-08-19

### Fixed
- `TURN_END` 阶段结算插入钢筋流血、生物伤害、Karma 回血、敏捷扣减后
  立即调用 `refresh_status + send_game_state`，避免「有播报无数值变化」。
  修改位置：`server.py` / `client.py` 的 `play_active_turn` 在
  `signal_turn_change` 之前补齐同步逻辑。
- 弃牌阶段弃 2 技能 + 1 物品后下一回合重复抽到相同 3 张牌：临时将
  「打出的非消耗牌 / 弃牌」从 `draw_pile.append`（堆顶）改为
  `draw_pile.insert(0, card_id)`（堆底）。本版本进一步用双堆循环彻底解决。
- 修复抽牌堆查看弹窗、手牌右键放大弹窗使用阻塞 `window.read()` 与主窗口
  事件泵冲突导致弹窗立即消失并卡住的问题：统一改为 `window.read(timeout=50)`
  轮询，处理 `TIMEOUT_EVENT` 继续等待，异常时退出避免死循环。

## [0.3.x] - 2026-08

### Added
- 蛞蝓猫角色完整卡组（技能 / 物品 / 见闻 / 生物 / 插入物 共 50 张）。
- Karma 回血、敏捷减伤（仅在实战扣血时消耗）、动能资源系统。
- 见闻牌按 ID 独立累计耗能减 1，有效成本低于卡面时显示深绿色。
- 管虫使见闻牌不占手牌位，手牌上限判定 / 弃牌达标 / 提示文本使用
  `effective_hand_size_for_limit`，UI 显示为「有效/总」双格式。

### Changed
- 生物牌（16-26）不再进入蛞蝓猫初始牌堆，仅通过见闻解锁或卡牌效果
  入手；解锁后的生物计数独立追踪并在牌堆查看器中以金色边框展示。
- 插入物（钢筋 49 / 电矛 50）以红色边框 + [Insert] 文本与普通手牌区分。
- 失去生命播报顺序：扣血 → 结算 Karma → 计算实际扣血量 → 仅在
  actual_loss > 0 时播报「失去生命」 → 触发 Karma 回血时追加「消耗业力
  重返雨中」。
